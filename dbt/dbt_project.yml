name: 'market_microstructure'
version: '1.0.0'
config-version: 2

profile: 'market_microstructure'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  market_microstructure:
    staging:
      +materialized: view
    marts:
      +materialized: table
